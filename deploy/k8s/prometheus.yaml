apiVersion: v1
kind: ConfigMap
metadata:
  name: ai-job-prometheus
data:
  prometheus.yml: |
    scrape_configs:
      - job_name: ai-job-orchestrator
        static_configs:
          - targets: ["ai-job-api-server:8080"]
