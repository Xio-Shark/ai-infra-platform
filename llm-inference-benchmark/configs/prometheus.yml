global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/rules/rules.yml

scrape_configs:
  - job_name: llm-bench-api
    static_configs:
      - targets:
          - host.docker.internal:8080
    metrics_path: /metrics
